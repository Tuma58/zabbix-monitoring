# Custom configs managed by AIMon assistant
#
# Files here are visible to the AI via tools list_configs / read_config /
# analyze_config / patch_config / write_config.
# Prefer creating snippets under this folder (e.g. snmp-notes.txt).
